export class Facture {
    constructor(
        id: number,
        code: number,
        tva: number,
        nom_societe: string,
        quantite: number,
        articleFac_id: number,
    ) { }
}
